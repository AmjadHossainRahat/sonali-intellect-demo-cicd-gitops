# Demo Script

1. Open `kind/cluster-config.yaml`.
2. Run `.\scripts\create-cluster.ps1` on Windows or `./scripts/create-cluster.sh` on Git Bash/Linux/macOS.
3. Run `kubectl get nodes -o wide`.
4. Open Lens and select `kind-cicd-gitops-demo`.
5. Show Nodes, Namespaces, and system Pods.
