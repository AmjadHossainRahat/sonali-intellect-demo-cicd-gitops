# Troubleshooting

If Lens cannot connect, run `kubectl config current-context` and select `kind-cicd-gitops-demo`. If Docker Desktop is stopped, Kind cannot create nodes. Docker Desktop's built-in Kubernetes does not need to be enabled for this lab.
