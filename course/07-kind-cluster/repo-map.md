# Repository Map

| File | Read by | Purpose |
|---|---|---|
| `kind/cluster-config.yaml` | Kind | Cluster topology |
| `scripts/create-cluster.ps1` or `.sh` | Instructor | Idempotent cluster creation |
| `scripts/bootstrap.ps1` or `.sh` | Instructor | Setup helper |
| `scripts/cleanup.ps1` or `.sh` | Instructor | Delete local cluster |
