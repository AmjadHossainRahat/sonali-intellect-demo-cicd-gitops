# Demo Script

1. Open `kubernetes/base/kustomization.yaml`.
2. Open `kubernetes/overlays/local/kustomization.yaml`.
3. Render base, then render local overlay.
4. Compare namespace and image digest behavior.

