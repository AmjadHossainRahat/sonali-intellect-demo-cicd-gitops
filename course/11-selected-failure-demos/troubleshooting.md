# Troubleshooting

If a failure script depends on a running cluster, first complete Lessons 07 through 10. If the recovery command cannot use Argo CD CLI, use the fallback kubectl path and explain the difference.

