# Recovery

Use the matching script in `scripts/recovery/` after each failure. For GitOps-managed resources, prefer restoring desired state in Git or triggering Argo CD sync so learners see the pull-based model.

