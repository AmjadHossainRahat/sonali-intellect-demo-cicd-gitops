# Demo Script

1. Open `InfoController.java` and `InfoControllerTest.java`.
2. Run `mvn clean test`.
3. Run `mvn clean package`.
4. Start `java -jar target/*.jar`.
5. Curl `/`, `/api/version`, and `/actuator/health/readiness`.

