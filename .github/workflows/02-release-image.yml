name: 02 - Release Image to Harbor

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: none
  id-token: write

concurrency:
  group: release-image-${{ github.ref }}
  cancel-in-progress: false

env:
  IMAGE_NAME: sonali-intellect-demo-cicd-gitops

jobs:
  publish:
    name: Build Once, Scan, and Publish
    runs-on: ubuntu-24.04
    outputs:
      image-digest: ${{ steps.digest.outputs.digest }}
      image-ref: ${{ steps.digest.outputs.image_ref }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Run tests
        run: mvn -B clean test

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Prepare metadata
        id: meta
        shell: bash
        run: |
          registry="${HARBOR_REGISTRY%/}"
          project="${HARBOR_PROJECT#/}"
          echo "tag=sha-${GITHUB_SHA}" >> "$GITHUB_OUTPUT"
          echo "repository=${registry}/${project}/${IMAGE_NAME}" >> "$GITHUB_OUTPUT"
        env:
          HARBOR_REGISTRY: ${{ vars.HARBOR_REGISTRY || secrets.HARBOR_REGISTRY || 'demo.goharbor.io' }}
          HARBOR_PROJECT: ${{ vars.HARBOR_PROJECT || secrets.HARBOR_PROJECT || 'si_demo_harbor' }}

      - name: Build image locally for scan
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: ${{ steps.meta.outputs.repository }}:${{ steps.meta.outputs.tag }}
          build-args: |
            BUILD_COMMIT=${{ github.sha }}

      - name: Scan final image
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ steps.meta.outputs.repository }}:${{ steps.meta.outputs.tag }}
          format: table
          severity: HIGH,CRITICAL
          exit-code: "0"

      - name: Generate SBOM
        uses: anchore/sbom-action@v0
        with:
          image: ${{ steps.meta.outputs.repository }}:${{ steps.meta.outputs.tag }}
          format: spdx-json
          output-file: sbom.spdx.json

      - name: Upload SBOM artifact
        uses: actions/upload-artifact@v7
        with:
          name: sbom-${{ github.sha }}
          path: sbom.spdx.json

      - name: Validate Harbor push credentials
        shell: bash
        run: |
          if [ -z "${HARBOR_USERNAME}" ] || [ -z "${HARBOR_PASSWORD}" ]; then
            echo "::error::Set GitHub Actions secrets HARBOR_USERNAME and HARBOR_PASSWORD from the Harbor ci-push robot account."
            exit 1
          fi
        env:
          HARBOR_USERNAME: ${{ secrets.HARBOR_USERNAME }}
          HARBOR_PASSWORD: ${{ secrets.HARBOR_PASSWORD }}

      - name: Login to Harbor
        uses: docker/login-action@v4
        with:
          registry: ${{ vars.HARBOR_REGISTRY || secrets.HARBOR_REGISTRY || 'demo.goharbor.io' }}
          username: ${{ secrets.HARBOR_USERNAME }}
          password: ${{ secrets.HARBOR_PASSWORD }}

      - name: Push image
        run: docker push "${{ steps.meta.outputs.repository }}:${{ steps.meta.outputs.tag }}"

      - name: Resolve digest
        id: digest
        shell: bash
        run: |
          digest="$(docker buildx imagetools inspect "${{ steps.meta.outputs.repository }}:${{ steps.meta.outputs.tag }}" --format '{{json .Manifest.Digest}}' | tr -d '"')"
          image_ref="${{ steps.meta.outputs.repository }}@${digest}"
          echo "digest=${digest}" >> "$GITHUB_OUTPUT"
          echo "image_ref=${image_ref}" >> "$GITHUB_OUTPUT"
          printf '%s\n' "${image_ref}" > image-digest.txt

      - name: Add immutable reference to Docker Build summary
        shell: bash
        run: |
          echo "Full immutable image reference:"
          echo "${{ steps.digest.outputs.image_ref }}"
          echo "::notice title=Full immutable image reference::${{ steps.digest.outputs.image_ref }}"

          {
            echo "### Docker Build summary"
            echo ""
            echo "**Full immutable image reference**"
            echo ""
            echo "\`\`\`text"
            echo "${{ steps.digest.outputs.image_ref }}"
            echo "\`\`\`"
            echo ""
            echo "Copy this value into workflow \`03 - Promote Image Digest to Local GitOps\`."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload digest artifact
        uses: actions/upload-artifact@v7
        with:
          name: image-digest-${{ github.sha }}
          path: image-digest.txt
